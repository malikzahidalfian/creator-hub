# Copy to .env.local for npm run dev. Never use a VITE_ prefix for server keys.
# Workspace opens directly without a password or session configuration.
# Project URL and server-only service_role / secret key from Supabase settings.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
